# ПЛАН ПРОЕКТА: Платформа для правок с картинками

## КТО Я И ЧТО ДЕЛАЮ
- Платон, 14 лет
- Создаю SaaS-проект для фрилансеров
- MVP за 1 неделю
- Цель: выйти к деньгам до конца лета

## ЧТО ЭТО ЗА ПРОЕКТ
Платформа для фрилансеров, где:
- Фрилансер загружает картинку (макет)
- Генерируется ссылка для клиента
- Клиент ставит текстовые метки на картинке
- Фрилансер видит метки в реалтайме
- Система кругов правок (лимиты)

## ТЕХНИЧЕСКИЙ СТАК
- Next.js 16 + React 19 + TypeScript
- Firebase (Firestore, Auth)
- Cloudinary (хранение картинок)
- Tailwind CSS
- Framer Motion

## СТРУКТУРА ПРОЕКТА
```
revio-source/
├── app/
│   ├── _components/       # UI компоненты
│   ├── _lib/             # утилиты
│   ├── page.tsx          # главная страница (мои проекты)
│   ├── login/            # авторизация
│   └── review/[id]/      # клиентская страница (без авторизации)
├── lib/
│   ├── firebase.ts       # конфиг Firebase
│   ├── auth.ts           # функции авторизации
│   ├── projects.ts      # модель проекта + функции
│   ├── markers.ts        # модель меток + функции
│   ├── cloudinary.ts     # функции Cloudinary
│   └── utils.ts          # утилиты
└── package.json
```

## 7 ШАГОВ MVP

### ✅ ШАГ 1: Редактировать модель проекта (ВЫПОЛНЕНО)
- Убраны лишние типы (video, site, brief, checklist)
- Оставлен только статус: active, locked, done
- Добавлены поля: imageUrls, currentRound, isLocked
- Создан файл lib/markers.ts (модель меток)
- Создан файл lib/cloudinary.ts
- Cloudinary добавлен в package.json

### 🔨 ШАГ 2: Загрузка картинок фрилансером (В ПРОЦЕССЕ)
- Компонент для загрузки файлов
- Интеграция с Cloudinary
- Обновление проекта (imageUrls)
- Предпросмотр загруженных картинок
- Возможность удаления картинок

### ⏳ ШАГ 3: Клиентская страница (без авторизации)
- Страница по ссылке /review/[id]
- Отображение картинки
- Проверка isLocked (клиент не может редактировать если заблокирован)
- Показ текущего круга (1/3)

### ⏳ ШАГ 4: Маячки (точечный + общий)
- Точечный: клик на картинку → появляется метка
- Общий: кнопка "Общий комментарий"
- Форма для ввода текста
- Сохранение в Firestore (realtime)
- Отображение меток на картинке

### ⏳ ШАГ 5: Система кругов правок
- Фрилансер ставит лимит кругов (3 по умолчанию)
- После отправки правок → isLocked = true
- Фрилансер загружает новую версию → currentRound++, isLocked = false
- Лимит исчерпан → показ сообщения limitMessage

### ⏳ ШАГ 6: Адаптивность и зум
- Zoom (колесико/пинч)
- Pan (перетаскивание)
- Стрелки ← → для переключения картинок
- Адаптивный дизайн (телефон + ПК)

### ⏳ ШАГ 7: Полировка и деплой
- Проверка всех шагов
- Исправление ошибок
- Деплой на Vercel
- Настройка Cloudinary переменных окружения

## МОДЕЛИ ДАННЫХ

### Project (lib/projects.ts)
```typescript
{
  id: string;
  ownerUid: string;
  name: string;
  description: string;
  imageUrls: string[];          // массив URL картинок (Cloudinary)
  currentRound: number;         // текущий круг правок
  roundsTotal: number;           // лимит кругов
  roundsLeft: number;            // осталось кругов
  limitMessage: string;         // сообщение при исчерпании лимита
  isLocked: boolean;            // заблокирован ли (клиент отправил правки)
  pinned: boolean;
  archived: boolean;
  icon: string;
  createdAt: Timestamp;
  updatedAt: Timestamp;
}
```

### Marker (lib/markers.ts)
```typescript
{
  id: string;
  projectId: string;
  round: number;                // номер круга правок
  type: "point" | "general";    // точечный или общий
  x?: number;                   // позиция X (0-1) для точечного
  y?: number;                   // позиция Y (0-1) для точечного
  text: string;                 // текст правки
  createdAt: Timestamp;
}
```

## ПОТОК ПРИЛОЖЕНИЯ

### Фрилансер:
1. Создает проект → указывает лимит кругов
2. Загружает картинки (Cloudinary)
3. Копирует ссылку /review/[id]
4. Отправляет клиенту

### Клиент:
1. Открывает ссылку /review/[id] (без авторизации)
2. Видит картинку + текущий круг (1/3)
3. Кликает на картинку → ставит точечный маркер
4. Или нажимает "Общий комментарий"
5. Пишет текст → маркер сохраняется (realtime)
6. Нажимает "Отправить правки" → проект блокируется

### Фрилансер:
1. Видит метки в реалтайме
2. Вносит правки
3. Загружает новую версию картинок
4. Круг увеличивается (2/3)
5. Ссылка снова активна

## МОНИТИЗАЦИЯ MVP
- Бесплатно: 1 проект
- 299₽/мес: безлимит проектов

## СЛЕДУЮЩИЕ ШАГИ ДЛЯ НОВОЙ СЕССИИ
1. Прочитать PROJECT_PLAN.md
2. Прочитать код в lib/projects.ts, lib/markers.ts, lib/cloudinary.ts
3. Продолжить с текущего шага
4. Не терять контекст - писать важные решения в этот файл

## ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ (нужно настроить)
Скопировать из .env.example в .env.local и заполнить:
- NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME
- NEXT_PUBLIC_CLOUDINARY_API_KEY  
- CLOUDINARY_API_SECRET
- Firebase переменные (должны быть уже)

## ДЛЯ ЗАПУСКА
```bash
npm install
npm run dev
```
Открыть http://localhost:3000

## ТЕКУЩИЙ СТАТУС
- ✅ ВСЕ 7 ШАГОВ ВЫПОЛНЕНЫ
- MVP готов к тестированию
- localhost не работает у Платона (проблема с Chrome)
- Нужно настроить переменные окружения (.env.local)
- Развернуть на Vercel для теста

## ЧТО НУЖНО СДЕЛАТЬ ПЕРЕД ТЕСТОМ
1. Настроить Cloudinary (получить ключи)
2. Заполнить .env.local переменными
3. Запустить `npm run dev`
4. Или развернуть на Vercel

## ИЗМЕНЕНИЯ В КОДЕ
- lib/projects.ts - обновлена модель проекта
- lib/markers.ts - новая модель меток
- lib/cloudinary.ts - функции Cloudinary
- app/_components/image-uploader.tsx - компонент загрузки
- app/_components/new-project-modal.tsx - обновлен для правок
- app/_components/marker-canvas.tsx - компонент маячков с зумом
- app/_components/project-hub.tsx - обновлен для правок
- app/review/[id]/page.tsx - клиентская страница