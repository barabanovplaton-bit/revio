# ПЛАН

Живой документ для памяти контекста. Обновляется по ходу работы. Это единый файл — сюда перенесено всё ценное из удалённого `PROJECT_PLAN.md` + актуальный статус. (`README.md` был шаблоном AI Studio — полезного в нём нет.)

---

## Проект

Revio — SaaS для фрилансеров-дизайнеров. Дизайнер загружает макеты, кидает клиенту ссылку, клиент без регистрации ставит правки (маячки) на картинках. Дизайнер видит их в реальном времени и отмечает сделанные.

Стек: Next.js 16 (App Router, Turbopack) + React 19 + TypeScript, Tailwind, Framer Motion, Firebase (Auth + Firestore), Cloudinary (картинки), Vercel (деплой, автодеплой после пуша).
Репозиторий: `barabanovplat-bit/revio` (настоящий — `C:\Users\Платон\Documents\GitHub\revio`). В `KODE\Revion\revio-source` — старый клон, там НЕ работать.

---

## Текущий статус: MVP v3 — пакетные правки клиента + фиксы холста + лендинг (собрано, не запушено)

Клиент собирает правки в черновик (localStorage), нажимает «Готово» — правки уходят пакетом (`clientSubmitted`). Форма правки — рядом с точкой, панель «Мои правки» слева (ПК) / снизу (телефон). Неавторизованные на главной видят лендинг. Есть обратная связь (плавающая кнопка → Firestore → скрытый `/feedback`). Pro покупается вручную через Telegram с готовым текстом.

---

## Принятые решения

- **Раунды правок**: «Заменить макеты» начинает новый раунд. Старые макеты и правки сохраняются в истории (`packageHistory`). Ничего не стирается.
- **«Заменить макеты» доступна, только когда клиент прислал правки** (есть маркер в раунде или `clientSubmitted`). Иначе кнопка приглушена; при нажатии — подсказка. Если раунды кончились — «Раунды исчерпаны».
- **«Скопировать все правки» — убрана** (не нужна).
- **Холст = тот же просмотрщик, что у клиента, но read-only** (нельзя добавлять/удалять): точки привязаны к ФОТО, зум/драг двигают фото вместе с точками, листание, тумблер «С маячками / Без маячков», левая панель (на телефоне — снизу) со списком правок.
- **Панель правок**: нумерация по порядку создания (вычисляется на лету — удаление не ломает), фильтры «Порядок / Страницы / Длина», галочка «сделано» (поле `done`), клик по строке — переход на страницу маркера + подсветка.
- **Маркеры привязаны к конкретной картинке** (`imageIndex`). Старые маркеры без `imageIndex` показываются на всех страницах (backward compat).
- **Клиент собирает правки в черновик** (localStorage, ключ `revio:draft:{projectId}`, событие `revio:draft-changed`): клик по фото → форма «Опишите правку» **рядом с точкой** (слева от точки, у левого края — справа; у верха/низа — сдвиг внутрь), кнопка «Общий комментарий» в шапке, панель «Мои правки (N)» **слева на ПК / сворачиваемая снизу на телефоне**, удаление своих правок. «Готово (N)» → подтверждение → batch в Firestore + `clientSubmitted` → модалка «Правки отправлены!». После отправки клиент заблокирован.
- **Холст**: форма не перекрывает фото (перенесена к точке), порог «движения мыши» для клика 6px, стрелки листания — SVG по центру кружка, предзагрузка всех картинок (не мигает при листании), картинка скрыта до загрузки.
- **Обратная связь**: плавающая кнопка внизу справа на всех страницах → модалка (тип: баг/идея/другое, текст, email) → Firestore `feedback`. Просмотр — скрытый `/feedback` (только владелец по `NEXT_PUBLIC_OWNER_UID`), отметка «прочитано».
- **Лендинг**: главная `/` для неавторизованных (hero + «как работает» + тарифы + FAQ + CTA), авторизованные видят дашборд.
- **Pro через Telegram**: кнопка «Оплатить» → ссылка `https://t.me/Gafti?text=...` с готовым текстом «Хочу купить Pro в Revio». Если у пользователя уже Pro — кнопка «У вас уже Pro».
- **Лимит на Free**: счётчик раундов при создании проекта (1–5, по умолчанию 5). Докупка раундов — суммарно не больше 5.
- **Лимит на Pro**: без ограничения раундов.
- **Маячки (правки)**: без лимита количества.
- **Pro включается вручную** через Telegram @Gafti (оплаты нет). Кнопки «Pro» ведут на `/pricing`.
- **Промо-блок при исчерпании** (Free, 5/5): «Раунды закончились» → «Перейти на Pro».
- Cloudinary-переменные должны быть заданы в Vercel (env) — иначе загрузка падает.

---

## Жизненный цикл проекта

```
Создание → Загрузка макетов → Ссылка активна →
Клиент тыкает в картинки и пишет комментарии (без регистрации) →
Клиент нажимает «Готово» (clientSubmitted=true) →
Дизайнер видит правки на холсте, отмечает сделанные →
«Заменить макеты» → новый раунд (правки в историю) →
...
```

## Страницы

- `/` — список проектов (для авторизованных) или лендинг (для гостей): поиск, карточки (название, счётчик изображений, дата), меню (переименовать/закрепить/удалить), визард создания со счётчиком раундов, модалка лимита Free (3 проекта).
- `/project/[id]` — кабинет: загрузка/замена макетов, ссылка для клиента, статус раундов, «Перейти на холст», «Заменить макеты», история версий, список правок раунда, список изображений.
- `/review/[id]` — страница клиента без регистрации: MarkerCanvas (клик по фото → форма у точки), «Общий комментарий», «Готово» → модалка «Правки отправлены!», блокировка после отправки.
- `/feedback` — скрытая страница отзывов для владельца (`NEXT_PUBLIC_OWNER_UID`).
- `/login` — авторизация (Google + email-ссылка).
- `/notifications` — уведомления.
- `/pricing` — тарифы (Free/Pro, FAQ), кнопка → Telegram.
- `/settings` — профиль (имя, тариф).

## Архитектура данных

### Project (`projects`)
| Поле | Тип | Описание |
|------|-----|----------|
| `ownerUid` | string | UID фрилансера |
| `name`, `description` | string | Название, описание |
| `clientName`, `clientContact` | string | Не используется в UI |
| `imageUrls` | string[] | Текущий пакет (Cloudinary) |
| `packageHistory` | ProjectPackage[] | Старые пакеты `{round, imageUrls, createdAt}` |
| `currentRound` | number | Текущий раунд правок |
| `roundsTotal`, `roundsLeft` | number | Всего / осталось раундов |
| `extraRoundsAdded` | number | Сколько докуплено |
| `clientSubmitted` | boolean | Клиент нажал «Готово» в текущем раунде |
| `limitMessage` | string | Текст при исчерпании |
| `isLocked` | boolean | Блокировка правок клиента |
| `pinned`, `deleted` | boolean | Закреплён / мягко удалён (слот лимита НЕ освобождается) |
| `status` | enum | `waiting_for_images` / `in_progress` / `exhausted` |
| `createdAt`, `updatedAt` | Timestamp | Даты |

### Marker (`markers`)
| Поле | Тип | Описание |
|------|-----|----------|
| `projectId` | string | Проект |
| `round` | number | Раунд |
| `type` | `point` / `general` | Точечная правка / общий комментарий |
| `x`, `y` | number | Позиция на фото (0-1, relative) |
| `imageIndex` | number | Номер картинки, к которой привязана точка |
| `done` | boolean | Отметил ли фрилансер как сделанную |
| `text` | string | Текст правки |
| `createdAt` | Timestamp | Дата (по ней — нумерация точек) |

### Notification (`notifications`)
| Поле | Тип | Описание |
|------|-----|----------|
| `ownerUid`, `projectId`, `projectName` | string | Адресат и проект |
| `type` | enum | `revisions_submitted` / `project_completed` |
| `message`, `read`, `createdAt` | … | Текст, прочитано, дата |

### UserProfile (`users`)
| Поле | Тип | Описание |
|------|-----|----------|
| `displayName`, `email` | string | Профиль |
| `plan` | `free` / `pro` | Тариф |
| `onboardingCompleted` | boolean | Прошёл онбординг |

### Feedback (`feedback`)
| Поле | Тип | Описание |
|------|-----|----------|
| `type` | `bug` / `idea` / `other` | Категория |
| `text` | string | Сообщение |
| `email` | string? | Контакт (опц.) |
| `userId` | string? | UID автора, если авторизован |
| `url` | string? | Страница, откуда отправлено |
| `read` | boolean | Прочитано владельцем |
| `createdAt` | Timestamp | Дата |

### Черновик клиента (localStorage, `lib/review-draft.ts`)
- Ключ `revio:draft:{projectId}`, событие `revio:draft-changed` (его шлют шапка review-страницы и MarkerCanvas).
- Элементы: `{ id, type: point|general, x, y, imageIndex, text, order }`.

## Тарифы

- **Free**: 3 проекта (считаются все созданные, включая удалённые), 10 изображений на проект, 5 раундов на проект (докупка до 5), маячки без лимита.
- **Pro**: безлимит проектов/изображений/раундов, 299 ₽/мес. Оплаты НЕТ — на кнопке «Оплатить» показывается Telegram (@Gafti): «напишите нам — включим Pro вручную».

---

## Что сделано

### Загрузка и Cloudinary
- [x] Маршрут `/api/upload` (server-side, Cloudinary), исправлен `.gitignore` (`/upload/`), работает на проде (env добавлены в Vercel).
- [x] Валидация env в route: понятная ошибка CONFIG_MISSING вместо «Must supply api_key».
- [x] `lib/cloudinary.ts`: сжатие (canvas), каскад качества, `uploadImageWithRetry` — ретраи только для временных ошибок (сеть/5xx), конфиг-ошибки не дублируются.
- [x] Двухфазная загрузка в хабе (prepare → upload, 2 воркера), блок ошибок `lastUploadErrors`.

### Раунды правок и тарифы
- [x] `lib/projects.ts`: `startNewRound()`, `hasRoundsLeft()`, `addExtraRounds(maxTotal)`, `getMaxRoundsForPlan()`, `canAddRounds()`.
- [x] Счётчик раундов в визарде создания (1–5 Free / до 100 Pro).
- [x] Докупка раунда (+1, разблокирует клиента), промо-блок при исчерпании → `/pricing`.
- [x] История версий с просмотром старых макетов и их правок.
- [x] Клиент: счётчик раундов, «Готово» → `clientSubmitted`, модалка «Правки отправлены!», блокировка при исчерпании + баннер.

### Просмотрщик (единый движок)
- [x] **`canvas-viewer.tsx`** — общий компонент для клиента и кабинета:
  - картинка рендерится в контейнере по её пропорциям, точки и клики считаются от ФОТО (не от экрана);
  - зум (колесо/кнопки) и драг двигают фото ВМЕСТЕ с точками;
  - точки с нумерацией по порядку создания, привязаны к своей странице (`imageIndex`);
  - листание ‹ ›, счётчик «N / M», тумблер «С маячками / Без маячков»;
  - панель правок (слева на ПК / снизу на телефоне): фильтры «Порядок / Страницы / Длина», галочки «сделано», клик по строке → переход к маркеру;
  - карточка выбранного маркера (текст + «сделано» / «удалить»);
  - мобильная версия — панель снизу.
- [x] `marker-canvas.tsx` — клиентская обёртка: клик по фото → форма «Опишите правку», «Общий комментарий», удаление своих правок.
- [x] Кабинет: «Перейти на холст» (read-only + панель), клик по карточке изображения — просмотр с панелью, история — тот же просмотрщик. Плашка «Клиент прислал правки».
- [x] «Заменить макеты» — видна всегда, активна только при наличии правок; подсказка при нажатии, пока правок нет.

### UI-мелочи
- [x] Убраны надписи «Изображение N» и «Просмотр», убран багнутый зум (+/−) из старого просмотрщика.
- [x] Текст клиенту без макетов: «Не переживайте, фрилансер скоро загрузит макеты».
- [x] `lib/markers.ts`: поля `imageIndex`, `done`; функция `toggleMarkerDone()`.

### Пакетные правки клиента + фиксы холста
- [x] **`lib/review-draft.ts`** — черновик в localStorage: `loadDraft/saveDraft/clearDraft/newDraftId`, событие `revio:draft-changed`.
- [x] `lib/markers.ts`: `createMarkers()` — запись пакета через `writeBatch`.
- [x] `lib/projects.ts`: `subscribeToProject()` — realtime-подписка на проект.
- [x] `marker-canvas.tsx` переписан: черновик, кнопка «Добавить», панель «Мои правки (N)» слева (ПК) / сворачиваемая снизу (моб), форма «Опишите правку» рядом с точкой (со сдвигом от краёв, stopPropagation от драга), удаление черновика.
- [x] `review/[id]/page.tsx`: подписка на проект, «Общий комментарий» (заметная кнопка с иконкой), «Готово (N)» → модалка подтверждения → batch + `clientSubmitted` → «Правки отправлены!», блокировка после отправки.
- [x] `canvas-viewer.tsx`: форма добавления рендерится внутри stage (рядом с точкой); предзагрузка всех картинок + скрытие текущей до загрузки (нет «ломки» при листании); стрелки листания — SVG по центру; порог драга 6px; сброс `dragMovedRef` в `endDrag`.
- [x] Кабинет (`project-hub.tsx`): без блоков «Правки раунда»/«Изображения», фикс file input, скролл-блокировка в оверлеях, «Заменить макеты» только при `clientSubmitted`, стрелки назад.

### Лендинг, обратная связь, Pro
- [x] `app/_components/landing.tsx` — лендинг для гостей на `/` (hero с имитацией холста, «Как это работает», тарифы, FAQ, CTA).
- [x] `lib/feedback.ts` + `app/_components/feedback-button.tsx` — плавающая кнопка → модалка → Firestore `feedback`.
- [x] `app/feedback/page.tsx` — скрытый просмотр отзывов (только владелец по `NEXT_PUBLIC_OWNER_UID`), отметка «прочитано».
- [x] `/pricing`: ссылка Telegram с готовым текстом «Хочу купить Pro в Revio (299 ₽/мес)», кнопка «У вас уже Pro» при активном Pro.

---

## В работе / очередь

- [ ] Запушить изменения (автодеплой на Vercel) — ждёт команды.
- [ ] **Firestore rules для `feedback`** (в консоли Firebase → Firestore → Rules): `allow create: if true;` (анонимно), `allow read, update, delete: if request.auth.uid in ['<OWNER_UID>']`. Без этого на проде отзывы не сохранятся (дефолт — deny).
- [ ] **Vercel env:** `NEXT_PUBLIC_OWNER_UID` — твой Firebase uid (иначе `/feedback` недоступен).
- [ ] Проверить на проде: клиентский холст (форма у точки, панель слева, маячки после фиксов, зум до края), лендинг, обратная связь, Pro-ссылка в Telegram.
- [ ] Уведомления (по плану — в последнюю очередь).
- [ ] (Потом, после валидации спроса) оплата ЮKassa / Telegram-бот для автовключения Pro, PDF-экспорт правок, SEO/OpenGraph, капча для обратной связи при спаме.

---

## Ссылки (для сборки/проверки)

- `npx tsc --noEmit` и `npm run build` должны оставаться чистыми (Vercel деплоит после пуша).
- Cloudinary env в Vercel: `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME`, `NEXT_PUBLIC_CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`.
- Внимание: в `.env.example` лежит реальный `CLOUDINARY_API_SECRET` — если репозиторий станет публичным, ключ надо сменить.
